{{#with interface}}
  export class {{ name }} {
    {{#if constructor}}constructor({{> arguments constructor }});{{/if}}
    {{#each operations}}
      {{ camelCase name }}({{> arguments }}): {{> type returnType }};
    {{/each}}
  }
{{/with}}
