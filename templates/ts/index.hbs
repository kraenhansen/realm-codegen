import bindings from "bindings";
const addon = bindings("{{ projectName }}");

{{#each interfaces}}
import type { {{name}} as {{name}}Class } from "./{{name}}";
{{/each}}

{{#each interfaces}}
export const {{name}}: typeof {{name}}Class = addon.{{name}};
{{/each}}
